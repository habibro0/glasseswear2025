<nav class="navbar shadow-sm sticky-top py-3" style="background: linear-gradient(45deg, #ff6f61, #f4f9ff);">
  <div class="container-fluid d-flex flex-wrap align-items-center justify-content-between">

    <!-- Logo -->
    <a class="navbar-brand me-4" href="/listings">
      <img src="https://res.cloudinary.com/dzp0yscrc/image/upload/v1743675911/logo_zc4pps.png" alt="Logo" class="navbar-logo">
    </a>

    <!-- All Items -->
    <ul class="navbar-nav flex-row align-items-center me-4">
      <li class="nav-item">
        <a class="nav-link fw-semibold text-dark fs-5 hover-link nav-item1 " href="/listings">All Items</a>
      </li>
    </ul>

    <!-- Search Bar -->
    <form class="d-flex mx-auto flex-grow-1 justify-content-center serrch" style="max-width: 500px;" action="/search" method="GET">
      <input class="form-control rounded-pill px-4 shadow-sm border-0" type="search" name="query" placeholder="Search for items..." style="width: 100%;">
    </form>

    <!-- Right Icons -->
    <ul class="navbar-nav flex-row align-items-center ms-3">

      <!-- Cart -->
      <li class="nav-item me-3 position-relative">
        <a href="/cart" class="text-dark nav-link fs-5">
          <i class="fa-solid fa-cart-shopping me-1"></i>
          <% if (typeof cartCount !== 'undefined' && cartCount > 0) { %>
            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
              <%= cartCount %>
            </span>
          <% } %>
        </a>
      </li>

      <!-- Favorites -->
      <li class="nav-item me-3">
        <a href="#" class="nav-link text-danger fs-5" id="favoritesBtn1">
          <i class="fa-solid fa-heart"></i>
        </a>
      </li>

      <!-- User -->
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-dark fs-5" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
          <i class="fa-solid fa-user"></i>
        </a>
        <ul class="dropdown-menu dropdown-menu-end shadow border-0" aria-labelledby="userDropdown">
          <% if (!currentUser) { %>
            <li><a class="dropdown-item" href="/signup">Sign Up</a></li>
            <li><a class="dropdown-item" href="/login">Login</a></li>
          <% } else { %>
            <li><a class="dropdown-item" href="/logout">Logout</a></li>
            <% if (["faisalmotiwala", "karim", "habibro123"].includes(currentUser.username)) { %>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/listings/new">Create Item</a></li>
              <li><a class="dropdown-item" href="/users">All Users</a></li>
              <li>
                <a class="dropdown-item d-flex justify-content-between align-items-center" href="/orders">
                  Orders
                  <% if (pendingOrders > 0) { %>
                    <span class="badge bg-danger ms-2"><%= pendingOrders %></span>
                  <% } %>
                </a>
              </li>
            <% } %>
          <% } %>
        </ul>
      </li>
    </ul>
  </div>
</nav>
